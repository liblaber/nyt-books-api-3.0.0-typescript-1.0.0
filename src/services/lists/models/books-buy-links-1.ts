// This file was generated by liblab | https://liblab.com/

import { z } from 'zod';

/**
 * The shape of the model inside the application code - what the users use
 */
export const booksBuyLinks1 = z.object({
  name: z.string().optional(),
  url: z.string().optional(),
});

/**
 *
 * @typedef  {BooksBuyLinks1} booksBuyLinks1
 * @property {string}
 * @property {string}
 */
export type BooksBuyLinks1 = z.infer<typeof booksBuyLinks1>;

/**
 * The shape of the model mapping from the api schema into the application shape.
 * Is equal to application shape if all property names match the api schema
 */
export const booksBuyLinks1Response = z
  .object({
    name: z.string().optional(),
    url: z.string().optional(),
  })
  .transform((data) => ({
    name: data['name'],
    url: data['url'],
  }));

/**
 * The shape of the model mapping from the application shape into the api schema.
 * Is equal to application shape if all property names match the api schema
 */
export const booksBuyLinks1Request = z
  .object({ name: z.string().nullish(), url: z.string().nullish() })
  .transform((data) => ({
    name: data['name'],
    url: data['url'],
  }));
